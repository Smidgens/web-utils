import { FC } from "react";
import styled from "styled-components";

export const TestTool1:FC = () => {
	return (
		<TestToolWrapper>

			TEST 1
		</TestToolWrapper>
	);
};

export const TestTool2:FC = () => {
	return (
		<TestToolWrapper>

			TEST 2
		</TestToolWrapper>
	);
};


const TestToolWrapper = styled.div`


	
`;